//import db  from "../config/database";

export const procesamientoService = {
    /*async create (id_imagen: number, id_expositor: number, comentarios: string, valido: boolean, IA_utilizada: string ) {

        const procesamiento = await db.procesados_imagenes.create({
            data: {
                id_imagen: id_imagen,
                id_expositor: id_expositor,
                comentarios: comentarios,
                valido: valido,
                IA_utilizada: IA_utilizada
            }
        });

        //falta crear respuesta en la base de datos con el procesamiento hecho y devolverlo
    }, */



}